package com.example.permissionmodule.repository;

import com.example.permissionmodule.entity.PermissionResponse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PermissionResponseRepository extends JpaRepository<PermissionResponse,Long> {
}
